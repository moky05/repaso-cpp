{
  "version": "2.0.0",
  "tasks": [{
    "label": "compilar archivo activo",
    "type": "cppbuild",
    "command": "/usr/bin/g++",
    "args": ["-std=c++17", "-Wall", "-Wextra", "-g", "${file}",
             "-o", "${fileDirname}/${fileBasenameNoExtension}"],
    "options": { "cwd": "${fileDirname}" },
    "problemMatcher": ["$gcc"],
    "group": { "kind": "build", "isDefault": true }
  }]
}
